package model;import java.util.Random;/** * Walkerのクラス * * 一つのrandom walkを表す */public class Walker {    private int x;//Walkerの位置    private final double p;//右に動く確率    private final Random random;    /**     * コンストラクタ     *     * @param x 初期位置     */    public Walker(int x, Random random) {        this(x, 0.5,random);//右への移動確率は1/2    }    /**     * コンストラクタ     *     * @param x 初期位置     * @param p 右への移動確率     */    public Walker(int x, double p, Random random) {        this.x = x;        this.p = p;        this.random =random;    }    /**     * 一時間ステップの移動     *     * @return 新しい位置     */    public int walk() {        double r = random.nextDouble();        /**         * 確率p でxを一つ増やす 確率1-p でxを一つ減らす        *         */        if (r < p) {            x++;        } else {            x--;        }        return x;    }    /**     * 現在の位置を返す     *     * @return     */    public int getX() {        return x;    }}